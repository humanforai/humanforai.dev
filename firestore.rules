rules_version = '2';

// All reads/writes go through the Cloud Function (Admin SDK bypasses rules).
// Client SDK access is fully locked down.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
